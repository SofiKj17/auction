<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
		xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.2.xsd">

	<changeSet id="create-table.user-monitors-lot" author="sofikj" dbms="postgresql">
		<sql splitStatements="false">
			create table user_monitors_lot
			(
				lot_id  BIGINT not null,
				user_id BIGINT not null
			);

			alter table user_monitors_lot
				add constraint PK_USER_MONITORS_LOT primary key (lot_id, user_id);

			create unique index user_monitors_lot_PK on user_monitors_lot (lot_id, user_id);


			create index user_monitors_lot2_FK on user_monitors_lot (user_id);

			create index user_monitors_lot_FK on user_monitors_lot (lot_id);

			alter table user_monitors_lot
				add constraint FK_USER_MONITOR_TO_LOT foreign key (lot_id)
					references lot (id)
					on delete restrict on update restrict;

			alter table user_monitors_lot
				add constraint FK_USER_MONITOR_TO_USER foreign key (user_id)
					references "user" (id)
					on delete restrict on update restrict;
		</sql>
	</changeSet>

</databaseChangeLog>